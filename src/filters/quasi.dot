"{{~ it.select('.QuasiElement') :child:index }}
  {{= child.ast.value.raw }}
  {{? it.select('expressions')[0] && it.select('expressions')[0].children[index] }}
    " + {{= it.select('expressions')[0].children[index].compile() }} + "
  {{?}}
{{~}}"